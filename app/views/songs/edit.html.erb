<h2>Edit Song</h2>
<%= form_for(@song) do |f| %>
  <%= f.label :name, "Name" %>
  <%= f.text_field :name %>

  <%= f.label :artist_id, "Artist" %>
  <%= f.text_area :artist_id %>

  <%= f.label :genre_id, "Genre" %>
  <%= f.text_area :genre_id %> 

  <%= f.submit %>
<% end %>
