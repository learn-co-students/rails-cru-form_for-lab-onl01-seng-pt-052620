<%= form_for @genre, url: { action: "create" } do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
  <%= f.submit "Create Genre", html: { class: "btn-submit" } %>
<% end %>
